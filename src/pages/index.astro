---
import { getCollection } from "astro:content";
import PlaylistLayout from "../layouts/PlaylistLayout.astro";

const songs = await getCollection("songs");
---

<PlaylistLayout songs={songs.map((song) => song.data)} />
